<h1>Users List</h1>

<% @users.each do |user| %>
  <div class="wrapper">
      <ul class="list-group set-width">
        <%= render partial: 'users/users_list', locals: { user: user } %>
    </ul>
  </div>
  <% end %>
  <% if @users.empty? %>
    <h1>There are no users to show</h1>
  <% end %>
